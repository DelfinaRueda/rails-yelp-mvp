<h1>Add a new review</h1>
<%= simple_form_for([@restaurant, @review]) do |f| %>

  <%= f.input :content, label: 'Your review content please', error: 'No text present' %>
  <%= f.input :rating,collection: [0,1,2,3,4,5], error: 'Not a number between 0-5' %>
  <%= f.button :submit %>
<% end %>
